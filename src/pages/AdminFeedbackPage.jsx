import { useCallback, useEffect, useState } from 'react'; import { useAuth } from '../contexts/AuthContext'; import { FEEDBACK_STATUSES, FEEDBACK_TYPES, listAdminFeedback, updateFeedbackStatus } from '../firebase/feedback'; import { formatFirestoreDate } from '../firebase/surveys';
export default function AdminFeedbackPage() { const { user, canManageUsers } = useAuth(); const [status, setStatus] = useState(''); const [items, setItems] = useState([]); const [error, setError] = useState(''); const load = useCallback(() => listAdminFeedback(status).then(setItems).catch((e) => setError(e.message)), [status]);
  useEffect(() => { if (canManageUsers) load(); }, [canManageUsers, load]); if (!canManageUsers) return <div className="empty-state">관리자만 의견을 관리할 수 있습니다.</div>; const change = async (item, next) => { try { await updateFeedbackStatus(item.id, item.status, next, user.uid); load(); } catch (e) { setError(e.message); } }; return <section><h1>의견 관리</h1><label className="field">상태 필터<select value={status} onChange={(e) => setStatus(e.target.value)}><option value="">전체</option>{Object.entries(FEEDBACK_STATUSES).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label>{error && <p className="form-error">{error}</p>}<div className="admin-list">{items.map((item) => <article className="admin-list-item" key={item.id}><strong>{FEEDBACK_TYPES[item.type]} · {FEEDBACK_STATUSES[item.status]}</strong><p>{item.content}</p><small>작성자: {item.createdByName || '이름 없음'} · 등록: {formatFirestoreDate(item.createdAt)}</small><small>{item.pageName}{item.surveyId ? ` · 설문 ${item.surveyId}` : ''}</small>{item.status === 'received' && <button className="secondary-button" onClick={() => change(item, 'reviewing')} type="button">확인 중으로 변경</button>}{item.status === 'reviewing' && <button className="secondary-button" onClick={() => change(item, 'completed')} type="button">완료로 변경</button>}</article>)}</div></section>; }
